#include "main.h"
/**
 *print_sign - prints a sign of the number given to the function
 *
 *@z: is the number to be checked
 *
 *Return: 1 and print + if z is greater than zero
 *and print 0 if z is zero
 *-1 and print - if z is less than zero
 *
 */
int print_sign(int z)
{
if (z > 0)
{
_putchar('+');
return (1);
}
else if (z == 0)

}
